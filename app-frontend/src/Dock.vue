<script setup>
defineProps({items:{type:Array,default:()=>[]},active:{type:String,default:''}})
defineEmits(['select'])
</script>
<template><div class="dock-wrap"><div class="dock" role="toolbar" aria-label="Ana bölümler"><button v-for="item in items" :key="item.id" :class="{active:active===item.id}" :aria-label="item.label" @click="$emit('select',item.id)"><span>{{ item.icon }}</span><small>{{ item.label }}</small></button></div></div></template>
<style scoped>.dock-wrap{position:fixed;z-index:45;right:0;bottom:16px;left:0;display:flex;justify-content:center;pointer-events:none}.dock{display:flex;align-items:flex-end;gap:7px;padding:8px;border:1px solid var(--border);border-radius:20px;background:var(--surface);box-shadow:0 18px 50px var(--shadow);backdrop-filter:blur(18px);pointer-events:auto;transition:background .3s ease,border-color .3s ease}.dock button{position:relative;display:grid;place-items:center;width:48px;height:48px;padding:0;border:1px solid var(--border);border-radius:14px;color:var(--text);background:var(--control);transition:width .24s cubic-bezier(.2,.9,.2,1),height .24s,transform .2s,background .18s}.dock button:hover{width:66px;height:66px;transform:translateY(-7px);background:var(--surface-blue)}.dock button:has(+button:hover),.dock button:hover+button{width:56px;height:56px}.dock button.active{color:var(--on-accent);background:var(--accent-gradient)}.dock button span{font-size:20px}.dock small{position:absolute;top:-28px;left:50%;padding:4px 7px;border-radius:6px;color:var(--text-strong);background:var(--surface-strong);box-shadow:0 6px 18px var(--shadow);font-size:10px;white-space:nowrap;opacity:0;transform:translateX(-50%) translateY(4px);transition:.18s}.dock button:hover small,.dock button:focus small{opacity:1;transform:translateX(-50%) translateY(0)}</style>
